import React, { Component } from 'react';
import { YT_API_KEY } from '../constants/credentials.js';

export default class TV extends Component {

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>This will evolve into a hitters web channel.</div>
    )
  }
}
